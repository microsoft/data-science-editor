---
title: Light level
services: lightlevel
tags: makecode
---

# Light level

The light level extension reports light level as a floating
point value between 0.0 and 100.0.
The `lightLevel` method returns the current light level.

The following program outputs the light level whenever the light level changes by 5 percent:

```blocks
modules.lightLevel1.onLightLevelChangedBy(5, () => {
    led.plotBarGraph(modules.lightLevel1.lightLevel(), 100)
})
```

<MakeCodeExtensionFooter serviceName="lightlevel" />